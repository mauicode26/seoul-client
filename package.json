{
  "name": "seoul",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "compile-mac": "bun build index.ts --target=bun-darwin-arm64 --outfile bin/seoul-client --compile",
    "compile-linux": "bun build index.ts --target=bun-linux-arm64 --outfile bin/seoul-client --compile"

  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/express": "^4.17.21",
    "chalk": "^5.3.0",
    "express": "^4.19.2"
  }
}